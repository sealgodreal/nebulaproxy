{
  "name": "rewriting-proxy",
  "version": "1.0.0",
  "description": "Generic rewriting web proxy: fetches a target page and rewrites all asset/link URLs to route back through the proxy.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.21.1"
  }
}
